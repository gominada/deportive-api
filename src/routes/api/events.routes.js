const router = require('express').Router()

router.get('',)
router.get('/',)
router.get('/date')
router.get('/upcoming')
router.get('/:eventId',)

router.post('/',)

router.put('/:eventId',)

router.delete('/:eventId',)

module.exports = router